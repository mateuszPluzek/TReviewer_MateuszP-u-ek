package com.example.treviewer.repositories;

import com.example.treviewer.models.StationPost;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StationPostRepository extends JpaRepository<StationPost, Integer> {
}
