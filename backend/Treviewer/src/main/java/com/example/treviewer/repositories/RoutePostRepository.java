package com.example.treviewer.repositories;

import com.example.treviewer.models.RoutePost;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RoutePostRepository extends JpaRepository<RoutePost, Integer> {
}
